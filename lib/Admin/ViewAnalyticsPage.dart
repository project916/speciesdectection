import 'package:flutter/material.dart';

class ViewAnalyticsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('View Analytics')),
      body: Center(
        child: Text('Here you can view analytics'),
      ),
    );
  }
}
