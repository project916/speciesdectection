import 'package:flutter/material.dart';

class ManageContentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Manage Content')),
      body: Center(
        child: Text('Here you can manage app content'),
      ),
    );
  }
}
