import 'package:flutter/material.dart';

class SendNotificationsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Send Notifications')),
      body: Center(
        child: Text('Here you can send notifications to users'),
      ),
    );
  }
}
