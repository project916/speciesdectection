import 'package:flutter/material.dart';

class UploadVideoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Video upload')),
      body: Center(child: Text('video upload page')),
    );
  }
}
